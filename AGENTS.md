# DSH 项目配置

```yaml
project_name: "{{ 项目名称 }}"
project_type: "{{ 项目类型：Web应用/移动端/桌面应用/数据分析/其他 }}"
tech_stack:
  frontend: "{{ 前端技术栈：如 Vue 3 + TypeScript + Ant Design Vue }}"
  backend: "{{ 后端技术栈：如 Midway.js / Spring Boot / Python FastAPI }}"
  database: "{{ 数据库：如 PostgreSQL / SQLite / MySQL }}"
  other: "{{ 其他技术：如 Docker / Redis / MinIO }}"
```

> 提示：上面的 YAML 块只是项目元数据占位，供人工阅读和填写；DSH 读取的是本文件的正文指令。

## 📋 项目概览

> **项目描述**：{{ 简要描述项目的核心功能和目标 }}
>
> **目标用户**：{{ 描述目标用户群体 }}
>
> **当前版本**：{{ v1.0.0 }}

---

## **重要**

- **讨论阶段只出方案，不动手**：除非我明确提出修改，否则一律只给方案/选项/影响分析，等我确认后才能改文件（详见硬约束 #27）
- 动手之前，先梳理需求和方案；复杂的改动先说明思路，再开始执行
- 遵循 AGENTS.md 中的硬约束与项目约定

## 🛠 技术栈

### 前端技术栈

{{ 前端技术栈描述，例如： }}

- **框架**：Vue 3 + TypeScript + Composition API
- **UI组件**：Ant Design Vue
- **状态管理**：Pinia
- **构建工具**：Vite
- **路由**：Vue Router

### 后端技术栈

{{ 后端技术栈描述，例如： }}

- **框架**：Midway.js + TypeScript
- **ORM**：TypeORM / Prisma
- **数据库**：SQLite3 / PostgreSQL
- **缓存**：Redis
- **认证**：JWT

### 其他技术

{{ 其他关键技术，例如：Docker、消息队列、对象存储等 }}

---

## 📂 项目结构

```
{{ 项目根目录 }}/
├── AGENTS.md                   # DSH 项目配置（本文件）
├── README.md                   # 项目总览和快速开始
├── .gitignore                  # Git 忽略配置
├── cordis.patch.yml            # MCP 服务器等配置（dsh web --patch 挂载）
├── .dsh/                       # DSH 项目级配置
│   └── skills/                 # 项目级技能库
│
├── docs/                       # 文档目录
│   ├── PRD.md                  # 产品需求文档
│   ├── ARCHITECTURE.md         # 系统架构设计
│   ├── API-SPEC.md             # API 接口规范
│   ├── DATABASE.md             # 数据库设计文档
│   ├── DEPLOY.md               # 部署文档
│   ├── CHANGELOG.md            # 变更日志
│   ├── PROJECT_STATUS.md       # 项目状态快照/索引（硬约束 #26，check-status.ps1 校验）
│   └── logs/
│       └── PROJECT_STATUS_LOG.md  # 状态明细日志（最新条目在最上方插入，按年分卷）
│
├── frontend/                   # 前端项目
│   ├── README.md               # 前端项目说明
│   ├── package.json            # 依赖配置
│   ├── src/                    # 源代码
│   │   ├── components/         # 公共组件
│   │   ├── views/              # 页面组件
│   │   ├── store/              # 状态管理
│   │   ├── api/                # API 接口
│   │   └── utils/              # 工具函数
│   └── public/                 # 静态资源
│
├── backend/                    # 后端项目
│   ├── README.md               # 后端项目说明
│   ├── package.json            # 依赖配置
│   ├── src/                    # 源代码
│   │   ├── controller/         # 控制器层
│   │   ├── service/            # 服务层
│   │   ├── entity/             # 数据模型
│   │   ├── middleware/         # 中间件
│   │   └── config/             # 配置文件
│   └── database/               # 数据库相关
│       └── migrations/         # 迁移脚本
│
├── docker/                     # Docker 配置
│   ├── docker-compose.yml      # 容器编排
│   ├── Dockerfile.frontend     # 前端镜像
│   └── Dockerfile.backend      # 后端镜像
│
└── scripts/                    # 工具脚本
    ├── init-db.sql             # 数据库初始化
    └── setup-env.sh            # 环境设置脚本
```

---

## 🎯 项目管理规范

### 状态追踪

- 状态文档分两文件（硬约束 #26）：
  - **`docs/PROJECT_STATUS.md`（快照/索引）**：只反映**当前**状态——项目概况、能力基线、最近变更、待开发项、阻塞问题；就地更新，不追加历史
  - **`docs/logs/PROJECT_STATUS_LOG.md`（明细日志）**：每次完成功能、代码、文档或配置修改后，在**表头下方第一行插入新条目**（最新条目在最上方，Keep a Changelog 惯例；持续增长按年分卷如 `PROJECT_STATUS_LOG-2026.md`）
- **读取协议（勿两文件通读）**：
  - 开工/写代码前只读**快照**（小文件，扫一眼即知当前能力、开发到哪、接口契约、阻塞）
  - 需查历史（某功能怎么迭代的、涉及哪些文件、为什么这么做）才读**日志**，且通常只读自己上次开工日期之后的新条目（文件顶部附近），无需通读
- 维护：当前态变化（新增/改动能力、待办、阻塞、概况）→ 更新快照相应小节；任何修改 → 在日志顶部插入一行
- `scripts/check-status.ps1` 可手动运行检查：改动了其他文件但快照与日志均未同步时输出告警（DSH 无钩子机制，需手动执行或在 CI 中调用）

### 文档管理

- 所有技术文档使用 Markdown 格式
- API 变更必须同步更新 `docs/API-SPEC.md`
- 数据库结构变更必须更新 `docs/DATABASE.md`

### Git 提交规范

```
<type>(<scope>): <subject>

type: feat(新功能), fix(修复), docs(文档), style(格式), refactor(重构), test(测试), chore(构建)
scope: 可选，影响范围，如 frontend/backend/auth
subject: 简短描述
```

---

## 🚀 技能（Skills）

技能位于 `.dsh/skills/`（项目级）或 `~/.dsh/skills/`（用户级）。DSH 会根据技能的描述自动触发合适的技能；也可以在对话中直接点名技能名称来强制使用。

### 📊 产品管理

| 技能                       | 适用场景                              | 输出位置 |
| -------------------------- | ------------------------------------- | -------- |
| **skills-product-manager** | 需求分析、PRD创作、产品规划、竞品分析 | `docs/`  |

### 💻 后端开发

| 技能                    | 适用场景                                  | 操作限制             |
| ----------------------- | ----------------------------------------- | -------------------- |
| **backend-midway**      | Midway.js + TypeScript + SQLite3 后端开发 | 仅限 `backend/` 目录 |
| **python-pro**          | Python 后端、数据分析、脚本开发           | 按项目约定           |
| **postgresql-designer** | 数据库设计、优化、迁移                    | `docs/DATABASE.md`   |

### 🎨 前端开发

| 技能                 | 适用场景                                     | 操作限制              |
| -------------------- | -------------------------------------------- | --------------------- |
| **frontend**         | Vue 3 + TypeScript + Ant Design Vue 前端开发 | 仅限 `frontend/` 目录 |
| **uniapp-architect** | UniApp 跨平台应用开发                        | 按项目约定            |
| **electron**         | Electron 桌面应用开发                        | 按项目约定            |

### ✨ UI/UX 设计

| 技能              | 适用场景                         |
| ----------------- | -------------------------------- |
| **ui-ux-pro-max** | 用户体验设计、交互优化、视觉规范 |
| **pencil-design** | Pencil 原型设计工具集成          |

### 📝 内容创作

| 技能                             | 适用场景                 |
| -------------------------------- | ------------------------ |
| **skills-ppt-designer**          | 演示文稿、汇报材料设计   |
| **skills-wechat-article-writer** | 公众号文章、技术博客写作 |

### 🔬 领域专业

| 技能      | 适用场景                               |
| --------- | -------------------------------------- |
| **space** | 航天、卫星遥感、遥感影像处理等领域支持 |

---

## ⚙️ MCP 服务器

MCP 服务器在项目根目录 `cordis.patch.yml` 中配置（启动时用 `dsh web --patch ./cordis.patch.yml` 挂载，可用 `dsh web --dump-config` 检查生效配置）：

| 服务（serverName） | 能力                          |
| ------------------ | ----------------------------- |
| docx               | Word 文档创建、填充、格式转换 |
| ragflow            | RAG 知识库检索、语义搜索      |
| pdf-reader         | PDF 内容提取、表格识别        |
| fetch              | HTTP 网络请求、API 调用       |
| excel              | Excel 表格读写、数据处理      |
| web-browsing       | 网页内容提取、元数据解析      |

---

## ⚠️ 硬约束（必须遵守）

### 🔒 安全约束

1. **禁止硬编码敏感信息**：API Key、密码、Token 等必须通过环境变量注入
2. **输入验证**：所有用户输入必须在后端进行验证，禁止信任前端验证
3. **SQL 注入防护**：使用 ORM / 参数化查询，禁止字符串拼接 SQL
4. **XSS 防护**：前端渲染用户输入内容必须进行转义处理
5. **认证鉴权**：所有非公开接口必须验证用户身份和权限
6. **日志脱敏**：日志中禁止输出密码、手机号、身份证等敏感信息

### 📦 代码质量约束

7. **TypeScript 严格模式**：所有 TypeScript 项目必须启用 `strict: true`
8. **禁止 `any` 类型**：除非特殊情况并添加注释，否则禁止使用 `any`
9. **错误处理**：所有异步操作必须包含错误捕获和处理逻辑
10. **代码重复率**：同一逻辑代码重复不得超过 2 次，抽取公共函数
11. **函数长度**：单个函数代码不超过 80 行，超过需拆分
12. **命名规范**：
    - 文件/文件夹：kebab-case（如 `user-profile.ts`）
    - 类名：PascalCase（如 `UserService`）
    - 函数/变量：camelCase（如 `getUserInfo`）
    - 常量：UPPER_SNAKE_CASE（如 `MAX_RETRY_COUNT`）

### 🎨 前端约束

13. **必须遵循 `frontend` 技能（强制前置，不可跳过）**：
    - **前置动作**：`frontend/` 目录内的一切改动，动手前**必须先读取并遵循 `.dsh/skills/frontend/SKILL.md`**；未加载技能就改前端代码视为违规（改 bug、调样式、重构等小改动同样不豁免，不以改动大小跳过）
    - **规范范围以技能为准**（本文件不重复）：页面拆分与组件提取、文件行数上限、JS/TS 语法规范、Composition API（`<script setup>`）、响应式与类型、**状态管理（非必要不用 store，仅复杂组件共享变量如全局 `map` / `viewer` 才入 store）**、**复用与拆分优先 composables**、**节制使用 `watch` / `computed`**、**新建 `.vue` 文件按模板结构创建**、样式作用域、异步错误处理、注释要求
    - **级别拉平**：技能中标注「（推荐）」的规范，在本项目中一律按**必须**执行
    - **冲突处理**：技能与本文件（含 #14–#16 前端铁律）不一致时取**更严格**者；两者结论相反且都很硬时，停下来问，禁止自行挑选
    - **技能不可用**（文件缺失/读取失败）：先说明情况，再按本文件 + 框架通用最佳实践执行，并在交付说明中标注
14. **跨组件共享状态使用 Pinia（非必要不用 store）**：仅复杂组件间共享的变量才进 store（如全局 `map` / `viewer`），禁止滥用全局状态；落地细则见技能 #10
15. **禁止在组件中直接修改 store 状态**：必须经由 store 暴露的 action 变更（技能未涵盖，保留在本文件）
16. **异步请求统一封装**：统一请求拦截器集中处理 Token、错误、loading（技能仅覆盖 try/catch 与加载/错误提示，封装层约定保留在本文件）

### 🖥️ 后端约束

17. **分层架构**：严格遵循 Controller → Service → Repository 分层
    - Controller：仅做参数校验、调用 Service、返回响应
    - Service：业务逻辑、事务控制
    - Repository：数据访问，禁止在 Service 中直接写 SQL
18. **接口规范**：
    - RESTful 风格：GET(查询) / POST(新增) / PUT(修改) / DELETE(删除)
    - 统一响应格式：`{ code: number, message: string, data: any }`
    - 统一错误码：业务错误码 ≥ 10000，系统错误 < 10000
19. **数据库约束**：
    - 所有表必须有主键、创建时间、更新时间字段
    - 关联字段必须加索引
    - 禁止物理删除，使用逻辑删除（`is_deleted` 字段）
20. **事务边界**：涉及多表写操作必须使用事务，保证数据一致性

### 📝 文档约束

21. **接口文档**：新增/修改接口必须同步更新 `docs/API-SPEC.md`
22. **数据库文档**：表结构变更必须同步更新 `docs/DATABASE.md`
23. **注释规范**：
    - 公共函数/类必须有 JSDoc 注释
    - 复杂业务逻辑必须添加行内注释说明思路
    - 特殊处理必须注释"为什么这么做"

### 🧪 测试约束

24. **核心逻辑必须有单元测试**：覆盖主要分支场景
25. **API 接口必须有集成测试**：覆盖正常流、异常流、边界条件
26. **项目状态追踪**：状态文档分两文件——`docs/PROJECT_STATUS.md`（快照/索引：当前概况、能力基线、最近变更、待办、阻塞，**就地更新不追加**）与 `docs/logs/PROJECT_STATUS_LOG.md`（明细日志：每次修改在**表头下方第一行插入**新条目，最新在最上，按年分卷）。每次完成功能、代码、文档或配置修改后：当前态变化须更新快照对应小节；任何修改须在明细日志顶部插入一行并保持快照「最后更新」同步。未同步状态文件视为任务未完成。

### 🤝 协作约束

27. **讨论阶段禁止动手改文件（先方案后确认）**：
    - **默认只讨论**：用户未明确提出修改（"改一下""实现""落地""帮我加上"等明确指令）时，一律视为讨论/评估阶段，只输出方案、选项、利弊与影响面分析，**不得修改任何文件**（代码、文档、配置、脚本、状态文件一视同仁）
    - **确认后才执行**：方案陈述完毕后停下等待用户确认，用户未确认前不开工；用户确认了 A 方案就不要顺手改 B
    - **讨论中的只读操作不受限**：搜索、读取、分析、给方案照常进行，限制的是"写"
    - **边界不清时先问**：需求含糊或可选路径多于一条时，先问再做，禁止按自己的猜测先改一版
    - **例外**：用户明确要求"边做边说"或授权自主执行时按授权执行；任何情况下都不得"先改了再解释"

---

## 💡 开发规范（建议遵守）

### 开发流程

1. 需求确认 → 接口设计 → 编码实现 → 自测 → 文档更新
2. 优先实现核心功能，再考虑优化和扩展
3. 遇到技术阻塞及时提出，不要卡在细节
4. 收尾：明细插入到 `docs/logs/PROJECT_STATUS_LOG.md` 表头下方第一行；当前态变化（能力/待办/阻塞等）同步更新 `docs/PROJECT_STATUS.md` 快照，确保与本次改动一致

### Code Review 关注点

- 功能是否正确实现
- 是否存在安全隐患
- 异常场景是否覆盖
- 代码可读性如何
- 是否需要补充文档

### 性能关注点

- N+1 查询问题
- 大接口分页处理
- 缓存使用合理性
- 前端首屏加载时间

---

## 🎯 AI 行为准则

### 沟通风格

- **语气**：专业、简洁、直接，避免冗余客套
- **提问**：一次最多问 3 个问题，先干活再确认细节
- **输出**：直接给代码/方案，少说"我来帮你..."这类废话

### 工作方式

1. **先看后写**：先读取现有文件了解项目结构和风格，再修改
2. **小步提交**：每次修改控制在合理范围，不要一次性改太多文件
3. **上下文感知**：保持代码风格与现有项目一致，不要引入不一致的风格
4. **主动思考**：发现潜在问题主动提出，不要等用户问
5. **使用技能**：任务与 `.dsh/skills/` 中某个技能的描述匹配时，先读取并使用对应技能
6. **收尾检查**：结束任务前核对状态文件（快照 `docs/PROJECT_STATUS.md` 或明细日志 `docs/logs/PROJECT_STATUS_LOG.md`）是否已随本次改动更新

### 输出格式

- 代码使用对应的语言标识：`typescript / `python / ```sql
- 复杂修改先说明思路再给代码
- 多文件修改按依赖顺序输出

---

## 📌 项目特有约定

> {{ 在此添加本项目特有的约定、特殊规则、临时约束等 }}
>
> 例如：
>
> - 本项目优先兼容 Chrome 浏览器
> - 数据库字段命名使用 snake_case
> - 时区统一使用 Asia/Shanghai

---
